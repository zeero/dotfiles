#!/bin/bash

option="-silent -buffer-name=grep -no-split -no-quit -auto-preview"

if [ $1 ]
then
  pattern="::$1"
fi

target="."
if [ $2 ]
then
  target=$2
fi

vim -c "setlocal splitbelow | Unite grep:$target$pattern $option"

