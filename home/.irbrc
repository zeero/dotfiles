# Require for standard library
require 'pp'

# Require for rubygems library
require 'rubygems'
require 'utility_belt'
# Ruby1.9以上の場合、utility_belt.rbの修正が必要
# -  IRB.conf[:IRB_RC] = lambda do
# +  IRB.conf[:IRB_RC] = lambda do |v|

