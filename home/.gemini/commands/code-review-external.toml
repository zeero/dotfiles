description = "外部プロセス(claude -p)を使用してコミット済みのコードをレビュー・修正させ、結果を検証します"
prompt = """
以下の手順に従って、コミットされたコードの外部レビュー、修正、およびその検証を行ってください。

【手順】
1. **対象コミットの特定**:
   - 引数 {{args}} が指定されている場合はそれを対象のコミットハッシュとします。
   - 指定がない場合（空文字列の場合）は、直近のコミット（HEAD）を対象とします。
   - 実行前に、対象となるコミット（ハッシュやHEADなど）をユーザーに明示してください。
   - run_shell_command で git show --summary <対象コミット> を実行し、コミットが存在することを確認してください。

2. **外部レビューと修正の依頼**:
   - run_shell_command を使用して、以下のコマンドを実行し、外部の claude にレビューと修正を依頼してください。
   ```bash
   claude -p "コミット対象の変更をレビューし、問題点があれば直接ファイルを編集して修正してください。
   対象コミット: $(git log -1 --format='%H' {{args:-HEAD}})
   変更内容:
   $(git show {{args:-HEAD}})

   指示:
   1. 上記のコミットの変更内容を分析してください。
   2. セキュリティ、パフォーマンス、可読性、アーキテクチャの観点からレビューを行ってください。
   3. 改善点が見つかった場合、利用可能なツールを使って該当ファイルを直接編集し、修正を適用してください。
   4. どのような問題をどのように修正したか、実行結果として簡潔に出力してください。"
   ```

3. **修正内容の検証**:
   - 外部プロセス（claude）による処理が完了した後、run_shell_command で git diff を実行し、ワークツリーにどのような変更が加えられたかを確認してください。
   - 変更内容（diff）を読み取り、以下の点を目視（静的分析）でチェックしてください。
     - 元のコードの意図が壊れていないか
     - 新たなバグや構文エラーが混入していないか
     - プロジェクトの規約に反していないか
   - 検証結果（変更の妥当性、問題の有無）をユーザーに報告してください。問題があればどのように直すべきかも提案してください。
"""
