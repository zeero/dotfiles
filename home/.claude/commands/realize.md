---
argument-hint: [対象ブランチ名（オプション）]
---

$ARGUMENTS

以下の手順で変更点を把握してください

**ブランチが指定されている場合**: 指定されたブランチとの差分を確認
**ブランチが指定されていない場合**: 以下の手順を実行:

1. 以下のコマンドでBASEブランチを特定します
```
git show-base-branch
```

2. ブランチ対象が正しいかユーザーに確認

3. BASEブランチとの差分を確認
```
git diff [BASE_BRANCH]
```

