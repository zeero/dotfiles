# .learnings — このリポジトリの学び（index）

このリポジトリで得た「次回から楽になる学び」の索引。本体は同ディレクトリの `<slug>.md`。
このファイルだけが `@.learnings/index.md` で常時ロードされる入口なので、1行要約に絞って短く保つ。追記は `learning` skill が行う。
本体の書式は `learning` skill の `<learning-entry-format>` を正とする。

## 学び一覧

（まだ無し。最初の学びが出たら `タイトル — <slug>.md — 1行要約` をこの下に追記する）
